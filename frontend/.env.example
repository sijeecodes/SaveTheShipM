# Matchmaking API (Lambda via API Gateway)
VITE_API_URL=

# WebSocket server (e.g. localhost:8080 for local dev)
VITE_SERVER_ENDPOINT=localhost:8080

# Set to "true" for local dev: force WebSocket to use localhost
VITE_USE_LOCAL_WS=true
